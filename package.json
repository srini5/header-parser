{
  "name": "header-parser",
  "version": "1.0.0",
  "description": "A microservice that parses the header sent in the request, and returns the IP address, language of the user and software.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "srini5",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
